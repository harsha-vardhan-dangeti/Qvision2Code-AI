# Optional: pick any one or many
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# For local LLaVA/Donut you won't need keys, just models.
# Pipeline knobs
UI2JSON_USE_YOLO=false
UI2JSON_USE_OCR=true
UI2JSON_USE_VLM=false
YOLO_MODEL=yakub/UI-Ultralytics-Det # replace with your UI-specific weights (or leave blank)